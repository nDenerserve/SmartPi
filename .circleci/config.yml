---
version: 2.1

executors:
  golang:
    docker:
    - image: cimg/go:1.18
    

jobs:
  build:
    executor: golang

    steps:
    - checkout
    - run: sudo apt-get update
    - run: sudo apt-get install -y libpam0g-dev
    - name: Run go mod
      working_directory: ./src
      run: go mod download
    - run: make style
    - run: make
    - store_artifacts:
        path: bin

workflows:
  version: 2
  build:
    jobs:
    - build
